<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hq.ecmp.mscore.mapper.ChinaCityMapper">

	<select id="queryCityCodeByCityName" parameterType="string"
		resultType="string">
		select CITY_CODE from china_city where CITY_NAME= #{cityName} or SHORT_NAME
		= #{cityName} limit 1
	</select>
	
	<select id="queryCityInfoListByCityName" 
	resultType="com.hq.ecmp.mscore.bo.CityInfo">
	select CITY_ID as cityId, CITY_CODE as cityCode,SHORT_NAME as cityName
	from
	china_city
	<where>
		1=1
		<if test="null !=cityName">
			and SHORT_NAME like concat('%',#{cityName},'%')
		</if>
	</where>
	</select>

	<select id="queryCityByCityCode" resultType="com.hq.ecmp.mscore.bo.CityInfo">
		select CITY_ID as cityId, CITY_CODE as cityCode,SHORT_NAME as cityName,PROVINCE_CODE as provinceCode
	from
	china_city where CITY_CODE=#{cityCode}
	</select>
</mapper>
